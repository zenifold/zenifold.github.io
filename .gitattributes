# Normalise line endings so the repo stays LF regardless of who checks it out.
# Without this, editing on Windows produces whole-file diffs on every commit.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
